use wavecore_render::DisplayCommand;
#[derive(Debug,Clone,Copy,PartialEq,Eq)]pub struct Rgba(pub u8,pub u8,pub u8,pub u8);pub struct Surface{pub width:u32,pub height:u32,pub pixels:Vec<Rgba>}
impl Surface{pub fn new(width:u32,height:u32)->Self{Self{width,height,pixels:vec![Rgba(255,255,255,255);(width*height)as usize]}}
pub fn paint(&mut self,list:&[DisplayCommand]){for c in list{match c{DisplayCommand::FillRect{rect,color}=>self.fill_rect(rect.x,rect.y,rect.width,rect.height,parse_color(color).unwrap_or(Rgba(240,240,240,255))),DisplayCommand::Border{rect,widths,color}=>{let c=parse_color(color).unwrap_or(Rgba(0,0,0,255));self.fill_rect(rect.x,rect.y,rect.width,widths.top,c);self.fill_rect(rect.x,rect.y+rect.height-widths.bottom,rect.width,widths.bottom,c);self.fill_rect(rect.x,rect.y,widths.left,rect.height,c);self.fill_rect(rect.x+rect.width-widths.right,rect.y,widths.right,rect.height,c);}DisplayCommand::Text{rect,font_size,..}=>self.fill_rect(rect.x,rect.y+font_size*.8,rect.width.min(font_size*12.0),font_size.max(1.0)/10.0,Rgba(30,30,30,255)),}}}
pub fn fill_rect(&mut self,x:f32,y:f32,w:f32,h:f32,color:Rgba){if w<=0.0||h<=0.0{return}let x0=x.max(0.0)as u32;let y0=y.max(0.0)as u32;let x1=(x+w).max(0.0).min(self.width as f32)as u32;let y1=(y+h).max(0.0).min(self.height as f32)as u32;for py in y0..y1{for px in x0..x1{self.pixels[(py*self.width+px)as usize]=color;}}}pub fn to_ppm(&self)->Vec<u8>{let mut o=format!("P6\n{} {}\n255\n",self.width,self.height).into_bytes();for Rgba(r,g,b,_)in &self.pixels{o.extend_from_slice(&[*r,*g,*b]);}o}}
fn parse_color(s:&str)->Option<Rgba>{let s=s.trim().to_ascii_lowercase();match s.as_str(){"transparent"=>Some(Rgba(0,0,0,0)),"white"=>Some(Rgba(255,255,255,255)),"black"=>Some(Rgba(0,0,0,255)),"red"=>Some(Rgba(255,0,0,255)),"green"=>Some(Rgba(0,128,0,255)),"blue"=>Some(Rgba(0,0,255,255)),"gray"|"grey"=>Some(Rgba(128,128,128,255)),_=>{let h=s.strip_prefix('#')?;match h.len(){3=>Some(Rgba(u8::from_str_radix(&h[0..1].repeat(2),16).ok()?,u8::from_str_radix(&h[1..2].repeat(2),16).ok()?,u8::from_str_radix(&h[2..3].repeat(2),16).ok()?,255)),6=>Some(Rgba(u8::from_str_radix(&h[0..2],16).ok()?,u8::from_str_radix(&h[2..4],16).ok()?,u8::from_str_radix(&h[4..6],16).ok()?,255)),8=>Some(Rgba(u8::from_str_radix(&h[0..2],16).ok()?,u8::from_str_radix(&h[2..4],16).ok()?,u8::from_str_radix(&h[4..6],16).ok()?,u8::from_str_radix(&h[6..8],16).ok()?)),_=>None}}}}
