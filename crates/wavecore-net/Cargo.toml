[package]
name = "wavecore-net"
version.workspace = true
edition.workspace = true

[dependencies]
ureq = { version = "2.10", default-features = false, features = ["tls", "gzip"] }
url = "2.5"
wavecore-storage = { path = "../wavecore-storage" }
